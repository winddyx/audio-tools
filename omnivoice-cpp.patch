diff --git a/CMakeLists.txt b/CMakeLists.txt
index e6a4eb9..709537f 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -166,7 +166,7 @@ link_ggml_backends(omnivoice-codec)
 
 # omnivoice-tts : full TTS pipeline. Grows phase by phase, currently load-only.
 add_executable(omnivoice-tts tools/omnivoice-tts.cpp)
-target_link_libraries(omnivoice-tts PRIVATE omnivoice-core)
+target_link_libraries(omnivoice-tts PRIVATE omnivoice-core yyjson)
 link_ggml_backends(omnivoice-tts)
 
 # tts-server : OpenAI-compatible HTTP server over the TTS pipeline.
diff --git a/src/omnivoice.cpp b/src/omnivoice.cpp
index 1336afe..b34cb0c 100644
--- a/src/omnivoice.cpp
+++ b/src/omnivoice.cpp
@@ -171,6 +171,16 @@ void ov_audio_free(struct ov_audio * a) {
     a->channels    = 0;
 }
 
+void ov_chunks_free(struct ov_chunk * chunks, int n_chunks) {
+    if (!chunks) {
+        return;
+    }
+    for (int i = 0; i < n_chunks; i++) {
+        std::free(chunks[i].text);
+    }
+    std::free(chunks);
+}
+
 void ov_init_default_params(struct ov_init_params * p) {
     p->abi_version = OV_ABI_VERSION;
     p->model_path  = nullptr;
@@ -207,6 +217,8 @@ void ov_tts_default_params(struct ov_tts_params * p) {
     p->on_chunk                = nullptr;
     p->on_chunk_user_data      = nullptr;
     p->postproc                = true;
+    p->chunks                  = nullptr;
+    p->n_chunks                = nullptr;
 }
 
 struct ov_context * ov_init(const struct ov_init_params * params) {
diff --git a/src/omnivoice.h b/src/omnivoice.h
index f18f9bd..c3efa33 100644
--- a/src/omnivoice.h
+++ b/src/omnivoice.h
@@ -54,7 +54,7 @@ extern "C" {
 // There is no separate semver triple. The runtime build identity is the
 // git short hash + commit date string returned by ov_version(); for
 // binding compat checks, OV_ABI_VERSION is the only number that matters.
-#define OV_ABI_VERSION 3
+#define OV_ABI_VERSION 4
 
 // Returns a static string of the form "<git-hash> (<date>)" identifying
 // the exact commit this binary was built from. Safe to call from any
@@ -93,6 +93,17 @@ struct ov_audio {
     int     channels;     // 1 (mono)
 };
 
+// One synthesised chunk reported by ov_tts_params.chunks. text is an owned
+// null-terminated UTF-8 string (malloc-allocated); release the whole array
+// with ov_chunks_free.
+struct ov_chunk {
+    char * text;
+};
+
+// Release a chunks array returned by ov_synthesize (each entry's text plus
+// the array itself). Safe on a NULL pointer / zero count.
+OV_API void ov_chunks_free(struct ov_chunk * chunks, int n_chunks);
+
 // Release the samples buffer and reset the struct to empty. Safe on a
 // zero-initialised struct (no double free, no NULL deref).
 OV_API void ov_audio_free(struct ov_audio * a);
@@ -247,6 +258,16 @@ struct ov_tts_params {
     // either way. Tail field: kept last for ABI growth, read only when
     // abi_version >= 3. The streaming path always post filters.
     bool postproc;
+
+    // Chunk metadata output (optional, buffered path). When both pointers
+    // are non-NULL and abi_version >= 4, the pipeline fills *chunks with a
+    // malloc'd array of ov_chunk and *n_chunks with its length, one owned
+    // UTF-8 text per synthesised chunk (the single-shot path reports a
+    // single chunk with the full text). The caller releases the array with
+    // ov_chunks_free. NULL disables the feature. Written through the
+    // pointers so the const params struct stays untouched.
+    struct ov_chunk ** chunks;
+    int *              n_chunks;
 };
 
 // Initialise to the standard defaults. Strings NULL, T_override 0,
diff --git a/src/pipeline-tts.cpp b/src/pipeline-tts.cpp
index 42c8ab8..d253674 100644
--- a/src/pipeline-tts.cpp
+++ b/src/pipeline-tts.cpp
@@ -845,7 +845,8 @@ static std::vector<float> tts_synthesize_long_internal(PipelineTTS *         pt,
                                                        int                   ext_ref_T,
                                                        float                 ref_rms,
                                                        const char *          dump_dir,
-                                                       tts_cancel *          cc) {
+                                                       tts_cancel *          cc,
+                                                       std::vector<std::string> * out_chunks) {
     if (tts_should_cancel(cc)) {
         return {};
     }
@@ -858,7 +859,12 @@ static std::vector<float> tts_synthesize_long_internal(PipelineTTS *         pt,
     int T_total = (T_override > 0) ? T_override : duration_estimate_tokens(text, ref_text, ext_ref_T);
 
     int  threshold_frames = (int) (chunk_threshold_sec * (float) frame_rate);
-    bool no_chunk         = (T_override > 0) || (chunk_duration_sec <= 0.0f) || (T_total <= threshold_frames);
+    // Hard-break newlines (strong ender + newline) are only honoured by the
+    // chunked path; force it so explicit line breaks never get collapsed by a
+    // single-shot pass, even for short texts.
+    bool has_hard_breaks = chunker_has_hard_break(text);
+    bool no_chunk        = (T_override > 0) || (chunk_duration_sec <= 0.0f)
+                           || ((T_total <= threshold_frames) && !has_hard_breaks);
 
     std::vector<float> audio;
 
@@ -878,6 +884,9 @@ static std::vector<float> tts_synthesize_long_internal(PipelineTTS *         pt,
         if (audio.empty()) {
             return audio;
         }
+        if (out_chunks) {
+            out_chunks->assign(1, chunker_strip(text));
+        }
     } else {
         // Per-chunk character budget derived from the full-text average
         // tokens-per-character, matching _generate_chunked upstream.
@@ -898,6 +907,9 @@ static std::vector<float> tts_synthesize_long_internal(PipelineTTS *         pt,
             ov_log(OV_LOG_ERROR, "[TTS-Long] chunker produced no chunks for input of %d chars", n_chars);
             return {};
         }
+        if (out_chunks) {
+            *out_chunks = chunks;
+        }
 
         ov_log(OV_LOG_INFO, "[TTS-Long] Chunked: %d chunks, T_total=%d frames, chunk_len=%d codepoints",
                (int) chunks.size(), T_total, chunk_len);
@@ -1138,7 +1150,9 @@ static ov_status tts_synthesize_long_stream_internal(PipelineTTS *         pt,
     int T_total = (T_override > 0) ? T_override : duration_estimate_tokens(text, ref_text, ext_ref_T);
 
     int  threshold_frames = (int) (chunk_threshold_sec * (float) frame_rate);
-    bool no_chunk         = (T_override > 0) || (chunk_duration_sec <= 0.0f) || (T_total <= threshold_frames);
+    bool has_hard_breaks  = chunker_has_hard_break(text);
+    bool no_chunk         = (T_override > 0) || (chunk_duration_sec <= 0.0f)
+                            || ((T_total <= threshold_frames) && !has_hard_breaks);
 
     uint32_t shared_ctr_lo = 0;
 
@@ -1493,10 +1507,11 @@ ov_status pipeline_tts_synthesize(PipelineTTS *         pt,
         postproc = params->postproc;
     }
 
+    std::vector<std::string> chunk_texts;
     std::vector<float> audio =
         tts_synthesize_long_internal(pt, pc, tok, text, lang, instruct, params->T_override, params->chunk_duration_sec,
                                      params->chunk_threshold_sec, params->denoise, postproc, mg_cfg, synth_ref_text,
-                                     synth_ref_tokens, synth_ref_T, synth_ref_rms, params->dump_dir, &cc);
+                                     synth_ref_tokens, synth_ref_T, synth_ref_rms, params->dump_dir, &cc, &chunk_texts);
 
     if (cc.triggered) {
         ov_set_error("ov_synthesize : cancelled by ov_cancel_cb");
@@ -1523,5 +1538,33 @@ ov_status pipeline_tts_synthesize(PipelineTTS *         pt,
     out->n_samples   = (int) audio.size();
     out->sample_rate = pc->sample_rate;
     out->channels    = 1;
+
+    // Chunk metadata (abi_version >= 4, buffered path only). Filled through
+    // the out-pointers so the const params struct is not mutated. Failure to
+    // allocate leaves *chunks NULL / *n_chunks 0; the caller then just has
+    // no segment info, the audio itself is unaffected.
+    if (params->abi_version >= 4 && params->chunks && params->n_chunks) {
+        *params->chunks   = nullptr;
+        *params->n_chunks = 0;
+        if (!chunk_texts.empty()) {
+            size_t n = chunk_texts.size();
+            struct ov_chunk * arr = (struct ov_chunk *) malloc(n * sizeof(struct ov_chunk));
+            if (arr) {
+                for (size_t i = 0; i < n; i++) {
+                    arr[i].text = nullptr;
+                    const std::string & s = chunk_texts[i];
+                    char * buf = (char *) malloc(s.size() + 1);
+                    if (buf) {
+                        memcpy(buf, s.data(), s.size());
+                        buf[s.size()] = '\0';
+                        arr[i].text   = buf;
+                    }
+                }
+                *params->chunks   = arr;
+                *params->n_chunks = (int) n;
+            }
+        }
+    }
+
     return OV_STATUS_OK;
 }
diff --git a/src/text-chunker.h b/src/text-chunker.h
index ff39ae7..e0eb3b4 100644
--- a/src/text-chunker.h
+++ b/src/text-chunker.h
@@ -53,6 +53,27 @@ static const std::set<std::string> & chunker_split_punctuation() {
     return s;
 }
 
+// Strong sentence-ending punctuation. A newline following one of these marks a
+// hard chunk break (see hard_after in chunk_text_punctuation): the sentence
+// closes its chunk even when it would otherwise merge into the next one.
+// Commas are excluded on purpose — Chinese copy wraps lines at commas all the
+// time and those must not fragment chunks.
+static const std::set<std::string> & chunker_hard_break_punctuation() {
+    static const std::set<std::string> s = {
+        ".",
+        "!",
+        "?",
+        ";",
+        ":",
+        "\xe3\x80\x82",  // U+3002 ideographic full stop
+        "\xef\xbc\x81",  // U+FF01 fullwidth exclamation mark
+        "\xef\xbc\x9f",  // U+FF1F fullwidth question mark
+        "\xef\xbc\x9b",  // U+FF1B fullwidth semicolon
+        "\xef\xbc\x9a",  // U+FF1A fullwidth colon
+    };
+    return s;
+}
+
 // Closing marks attach to the preceding sentence. Mirrors CLOSING_MARKS.
 static const std::set<std::string> & chunker_closing_marks() {
     static const std::set<std::string> s = {
@@ -235,6 +256,68 @@ static std::string chunker_strip(const std::string & s) {
 // Single source of truth for both the buffered and streaming paths.
 static constexpr int OMNIVOICE_MIN_CHUNK_LEN = 3;
 
+// Returns the last codepoint of a sentence that is neither whitespace nor a
+// closing mark (closing marks glue onto the sentence, e.g. "安心。”"), or an
+// empty string if none. This is the actual sentence-ending punctuation.
+static std::string chunker_last_non_ws(const std::vector<std::string> & cps) {
+    const std::set<std::string> & closing_set = chunker_closing_marks();
+    for (size_t i = cps.size(); i > 0; i--) {
+        const std::string & cp = cps[i - 1];
+        if (!chunker_is_unicode_whitespace(cp) && closing_set.count(cp) == 0) {
+            return cp;
+        }
+    }
+    return std::string();
+}
+
+// True if the leading whitespace run of a sentence contains a newline, i.e. a
+// line break sits between this sentence and the previous one.
+static bool chunker_leading_has_newline(const std::vector<std::string> & cps) {
+    for (const auto & cp : cps) {
+        if (!chunker_is_unicode_whitespace(cp)) {
+            return false;
+        }
+        if (cp == "\n") {
+            return true;
+        }
+    }
+    return false;
+}
+
+// True if text contains a hard break: a strong sentence-ending punctuation
+// followed (possibly after closing marks / whitespace) by a newline. Mirrors
+// the hard_after detection in chunk_text_punctuation; used to force the
+// chunked path so explicit line breaks are honoured even for short texts.
+static bool chunker_has_hard_break(const std::string & text) {
+    const std::set<std::string> & hard_set    = chunker_hard_break_punctuation();
+    const std::set<std::string> & closing_set = chunker_closing_marks();
+
+    const unsigned char * p   = (const unsigned char *) text.data();
+    const unsigned char * end = p + text.size();
+
+    bool pending = false;  // last non-whitespace codepoint was a hard ender
+    while (p < end) {
+        int n = chunker_utf8_len(*p);
+        if (p + n > end) {
+            n = (int) (end - p);
+        }
+        std::string cp((const char *) p, (size_t) n);
+        p += n;
+
+        if (chunker_is_unicode_whitespace(cp)) {
+            if (pending && cp == "\n") {
+                return true;
+            }
+            continue;
+        }
+        if (closing_set.count(cp) > 0) {
+            continue;  // closing marks glue to the previous sentence
+        }
+        pending = hard_set.count(cp) > 0;
+    }
+    return false;
+}
+
 // Splits text on sentence-ending punctuation (skipping abbreviations) and
 // merges sentences into chunks of at most chunk_len codepoints. If
 // min_chunk_len > 0, undersized chunks are merged with a neighbour.
@@ -302,13 +385,28 @@ static std::vector<std::string> chunk_text_punctuation(const std::string & text,
         sentences.push_back(current);
     }
 
+    // Hard breaks: a newline after strong sentence-ending punctuation forces a
+    // chunk boundary. hard_after[i] == true means sentence i must close its
+    // chunk even when the merge rule would keep it together with the next one.
+    const std::set<std::string> & hard_set = chunker_hard_break_punctuation();
+    std::vector<bool>             hard_after(sentences.size(), false);
+    for (size_t i = 1; i < sentences.size(); i++) {
+        if (chunker_leading_has_newline(sentences[i])
+            && hard_set.count(chunker_last_non_ws(sentences[i - 1])) > 0) {
+            hard_after[i - 1] = true;
+        }
+    }
+
     // Step 2: greedy merge of sentences into chunks of at most chunk_len
     // codepoints. A sentence that does not fit starts a new chunk by itself,
-    // even if it is longer than chunk_len.
+    // even if it is longer than chunk_len. A sentence marked as a hard break
+    // always closes the current chunk.
     std::vector<std::vector<std::string>> merged;
+    std::vector<bool>                     merged_hard;  // chunk was closed by a hard break
     std::vector<std::string>              cur_chunk;
 
-    for (const auto & sent : sentences) {
+    for (size_t i = 0; i < sentences.size(); i++) {
+        const auto & sent = sentences[i];
         if ((int) (cur_chunk.size() + sent.size()) <= chunk_len) {
             for (const auto & c : sent) {
                 cur_chunk.push_back(c);
@@ -316,43 +414,66 @@ static std::vector<std::string> chunk_text_punctuation(const std::string & text,
         } else {
             if (!cur_chunk.empty()) {
                 merged.push_back(cur_chunk);
+                merged_hard.push_back(false);
             }
 
             cur_chunk = sent;
         }
+
+        if (hard_after[i] && !cur_chunk.empty()) {
+            merged.push_back(cur_chunk);
+            merged_hard.push_back(true);
+            cur_chunk.clear();
+        }
     }
 
     if (!cur_chunk.empty()) {
         merged.push_back(cur_chunk);
+        merged_hard.push_back(false);
     }
 
     // Step 3: merge undersized chunks. The first chunk, if short, is folded
     // into the second. Subsequent short chunks fold into the previous one.
+    // Folding never crosses a hard-break boundary so explicit line breaks
+    // survive even when the surrounding chunks are tiny.
     std::vector<std::vector<std::string>> finals;
     if (min_chunk_len > 0) {
         bool first_short = !merged.empty() && (int) merged[0].size() < min_chunk_len;
 
+        // Index in `merged` that finals.back() was created from; the boundary
+        // after that chunk is hard iff merged_hard[last_merged_idx].
+        size_t last_merged_idx = 0;
+
         for (size_t i = 0; i < merged.size(); i++) {
             const auto & chunk = merged[i];
 
-            if (i == 1 && first_short) {
+            if (i == 1 && first_short && !merged_hard[0]) {
                 for (const auto & c : chunk) {
                     finals.back().push_back(c);
                 }
+                last_merged_idx = 1;  // chunk 1 was absorbed; its boundary is next
                 continue;
             }
 
             if ((int) chunk.size() >= min_chunk_len) {
                 finals.push_back(chunk);
+                last_merged_idx = i;
                 continue;
             }
 
             if (finals.empty()) {
                 finals.push_back(chunk);
+                last_merged_idx = i;
+            } else if (merged_hard[last_merged_idx]) {
+                // Hard-break boundary: keep the undersized chunk standalone so
+                // the explicit newline break is not folded away.
+                finals.push_back(chunk);
+                last_merged_idx = i;
             } else {
                 for (const auto & c : chunk) {
                     finals.back().push_back(c);
                 }
+                last_merged_idx = i;  // chunk i was absorbed; its boundary is next
             }
         }
     } else {
diff --git a/tools/omnivoice-tts.cpp b/tools/omnivoice-tts.cpp
index 9a28367..66993bc 100644
--- a/tools/omnivoice-tts.cpp
+++ b/tools/omnivoice-tts.cpp
@@ -23,6 +23,7 @@
 #include "utf8.h"
 #include "version.h"
 #include "voice-design.h"
+#include "yyjson.h"
 
 #include <algorithm>
 #include <cmath>
@@ -75,7 +76,9 @@ static void print_usage(const char * prog) {
             "  --no-preprocess-prompt  Skip ref-wav silence trim and ref-text terminal punctuation\n"
             "  --chunk-duration <sec>  Long-form chunk duration (default: 15.0, <= 0 disables chunking)\n"
             "  --chunk-threshold <sec> Activate chunking above this estimated duration (default: 30.0)\n"
-            "  --stream-by-line        Flush synthesis at each newline, one WAV header per line (-o '-')\n\n"
+            "  --stream-by-line        Flush synthesis at each newline, one WAV header per line (-o '-')\n"
+            "  --chunks-out <path>     Write the synthesis segmentation (JSON array of chunk\n"
+            "                          texts) to a sidecar file (buffered path only)\n\n"
             "Debug:\n"
             "  --no-fa                 Disable flash attention\n"
             "  --clamp-fp16            Clamp hidden states to FP16 range\n"
@@ -370,6 +373,10 @@ static int run_srt_dub(ov_context *                 ov,
 // Full TTS synthesis path via the OmniVoice handle. Lives outside main so the
 // debug paths (--llm-test, --maskgit-test) keep their lower-level init flow
 // completely untouched.
+// Forward declaration: writes the chunk-text sidecar (defined after
+// run_tts_via_ov).
+static bool write_chunks_json(const char * path, const struct ov_chunk * chunks, int n_chunks);
+
 static int run_tts_via_ov(const char * model_path,
                           const char * codec_path,
                           bool         use_fa,
@@ -390,6 +397,7 @@ static int run_tts_via_ov(const char * model_path,
                           uint64_t     seed_resolved,
                           const char * dump_dir,
                           const char * output_path,
+                          const char * chunks_out_path,
                           WavFormat    wav_fmt) {
     ov_init_params iparams;
     ov_init_default_params(&iparams);
@@ -644,6 +652,15 @@ static int run_tts_via_ov(const char * model_path,
     params.ref_text         = ref_text.c_str();
     params.dump_dir         = dump_dir;
 
+    // Optional chunk metadata sidecar: the pipeline fills chunk texts (C++
+    // segmentation, single-shot = one chunk) when requested; written as a
+    // JSON array of strings so Python recovers the segmentation without a
+    // persistent process.
+    struct ov_chunk * chunk_arr = nullptr;
+    int              n_chunks   = 0;
+    params.chunks  = &chunk_arr;
+    params.n_chunks = &n_chunks;
+
     ov_audio audio = {};
     if (ov_synthesize(ov, &params, &audio) != OV_STATUS_OK) {
         rc = 1;
@@ -652,12 +669,39 @@ static int run_tts_via_ov(const char * model_path,
     } else {
         fprintf(stderr, "[OmniVoice-TTS] TTS: wrote %s (%d samples @ %d Hz, %.2f s)\n", output_path, audio.n_samples,
                 audio.sample_rate, (double) audio.n_samples / (double) audio.sample_rate);
+        if (chunks_out_path) {
+            write_chunks_json(chunks_out_path, chunk_arr, n_chunks);
+        }
     }
     ov_audio_free(&audio);
+    ov_chunks_free(chunk_arr, n_chunks);
     ov_free(ov);
     return rc;
 }
 
+// Write chunk texts (JSON array of strings) to a sidecar file so the one-shot
+// caller can recover the segmentation. Returns false on write failure.
+static bool write_chunks_json(const char * path, const struct ov_chunk * chunks, int n_chunks) {
+    yyjson_mut_doc * doc = yyjson_mut_doc_new(NULL);
+    yyjson_mut_val * arr = yyjson_mut_arr(doc);
+    yyjson_mut_doc_set_root(doc, arr);
+    for (int i = 0; i < n_chunks; i++) {
+        yyjson_mut_arr_add_str(doc, arr, (chunks && chunks[i].text) ? chunks[i].text : "");
+    }
+    const char * json = yyjson_mut_write(doc, 0, NULL);
+    bool ok = false;
+    if (json) {
+        FILE * f = fopen(path, "wb");
+        if (f) {
+            ok = fwrite(json, 1, strlen(json), f) == strlen(json);
+            fclose(f);
+        }
+        free((void *) json);
+    }
+    yyjson_mut_doc_free(doc);
+    return ok;
+}
+
 static int main_impl(int argc, char ** argv) {
     if (argc <= 1) {
         print_usage(argv[0]);
@@ -691,6 +735,7 @@ static int main_impl(int argc, char ** argv) {
     int          mg_steps               = 0;
     const char * dump_dir               = NULL;
     WavFormat    wav_fmt                = WAV_S16;
+    const char * chunks_out_path        = NULL;
 
     for (int i = 1; i < argc; i++) {
         if (strcmp(argv[i], "--model") == 0 && i + 1 < argc) {
@@ -721,6 +766,8 @@ static int main_impl(int argc, char ** argv) {
             chunk_threshold_sec = (float) atof(argv[++i]);
         } else if (strcmp(argv[i], "--stream-by-line") == 0) {
             stream_by_line = true;
+        } else if (strcmp(argv[i], "--chunks-out") == 0 && i + 1 < argc) {
+            chunks_out_path = argv[++i];
         } else if (strcmp(argv[i], "--srt") == 0 && i + 1 < argc) {
             srt_path = argv[++i];
         } else if (strcmp(argv[i], "--ref-wav") == 0 && i + 1 < argc) {
@@ -806,6 +853,14 @@ static int main_impl(int argc, char ** argv) {
         fprintf(stderr, "[CLI] ERROR: --srt derives per cue duration from the SRT, drop --duration\n");
         return 1;
     }
+    if (chunks_out_path && stream_by_line) {
+        fprintf(stderr, "[CLI] ERROR: --chunks-out is incompatible with --stream-by-line\n");
+        return 1;
+    }
+    if (chunks_out_path && srt_path) {
+        fprintf(stderr, "[CLI] ERROR: --chunks-out is incompatible with --srt\n");
+        return 1;
+    }
 
     // Resolve sampling seed: -1 picks a fresh random seed from std::random_device,
     // any other value is used verbatim for reproducible runs across the maskgit
@@ -819,7 +874,7 @@ static int main_impl(int argc, char ** argv) {
         return run_tts_via_ov(model_path, codec_path, use_fa, clamp_fp16, ref_wav_path, ref_rvq_path, ref_text_path,
                               prompt_lang, prompt_instruct, prompt_duration_sec, prompt_denoise, preprocess_prompt,
                               chunk_duration_sec, chunk_threshold_sec, stream_by_line, srt_path, mg_steps,
-                              seed_resolved, dump_dir, output_path, wav_fmt);
+                              seed_resolved, dump_dir, output_path, chunks_out_path, wav_fmt);
     }
 
     BackendPair bp = backend_init("LM");
