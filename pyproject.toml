[project]
name = "audio-tools"
version = "0.2.0"
description = "audio-tools 配音工具：语音克隆（audiocpp 引擎，支持 OmniVoice / IndexTTS-2.5 多 TTS 模型 + SenseVoice ASR）"
requires-python = ">=3.10"
dependencies = [
    "soundfile",
    "numpy",
    "huggingface_hub",
    # Web 界面（gradio）
    "gradio==6.24.0",
]
